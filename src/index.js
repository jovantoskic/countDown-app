import React from "react";
import ReactDOM from "react-dom";
import App from "./App";


//povezivanje sa html elementom  kom se renderuje app, u ovom slucaju "root"
ReactDOM.render(
  <App />, document.getElementById('root')
);